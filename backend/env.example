# Railway環境変数
# PORT は Railway が自動設定するため、通常は設定不要
PORT=8000

# CORS設定（本番環境用）
# フロントエンドのURLを指定（例: https://your-frontend.railway.app）
# 開発環境では "*" を使用
ALLOWED_ORIGINS=https://your-frontend-service.railway.app

# アプリケーション設定
ENVIRONMENT=production 